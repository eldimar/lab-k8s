---
# type: ClusterIP - ver comentario em 09-vote-service.yaml: exposicao
# externa agora via Ingress (15-ingress.yaml).
apiVersion: v1
kind: Service
metadata:
  name: result
  namespace: voting-app
  labels:
    app.kubernetes.io/name: result
    app.kubernetes.io/part-of: voting-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: result
  ports:
    - port: 80
      targetPort: 80
      name: http
